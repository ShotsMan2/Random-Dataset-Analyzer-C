# Random-Dataset-Analyzer-C
A C algorithm that generates a constrained random dataset (split ranges with min diff), normalizes values below the average, performs sorting, and identifies prime numbers.
